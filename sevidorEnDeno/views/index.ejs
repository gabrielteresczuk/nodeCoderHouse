<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    
    <link rel="stylesheet" href="/views/style.css">
    <meta name="author" content="Teresczuk Gabriel">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2280%22>🤙</text></svg>">
    <title>Color - DENO</title>
</head>
<body>
  <main id="app">
    <div class="contenedor">
        <div class="titulo">
            <h1>Color</h1>
            <span class="material-symbols-outlined icon_size">palette</span>
        </div>
        
        <p class="texto_ayuda">ingrese un color en ingles</p>
        
        <form method="post" action="/color" id="color_form" class="color_form" autocomplete="off">
            <label for="color">Color:</label>
            <input type="text" name="color" id="color" placeholder="red" required class="input">
            <input type="submit" value="Ingresar" class="btn">
        </form>
    </div>

    <% if(data){ %>
    <div class="contendor_colores">
      <h2>Colores</h2>

        <ul class="colores_ul">
          <% for (item of data) { %>
            <li class="colores_li" style='color:<%= item.color %>'> <%= item.color %> <a rel="nofollow" data-confirm="Are you sure?" data-method="delete" href='/delete/<%= item.id %>'>borrar</a></li>
          <% } %>
        </ul>
    </div>
      <% } %>
</main>
  </body>
</html>